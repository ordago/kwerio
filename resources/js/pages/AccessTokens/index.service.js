import { createAsyncThunk } from "@reduxjs/toolkit"

export const PREFIX = "ACCESS_TOKEKNS"

export const metadata = createAsyncThunk(`${PREFIX}/metadata`, async () => {
  try {

  }

  catch (err) {

  }
})
