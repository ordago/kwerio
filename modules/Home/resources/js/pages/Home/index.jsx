import { Typography } from "@material-ui/core"
import React from "react"

function Home() {
  return (
    <Typography variant="caption">Home module</Typography>
  )
}

export default React.memo(Home)
